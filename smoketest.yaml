# Smoketest: minimal nginx tool to verify the scaffold → Fleet → ingress pipeline.
name: smoketest
ttl_days: 1
db: false
components:
  - name: app
    image: nginxdemos/hello:plain-text
    port: 80
    public: true
